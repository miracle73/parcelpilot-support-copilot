const $=s=>document.querySelector(s),chat=$('#chat'),input=$('#input');let token;const esc=s=>{let d=document.createElement('div');d.textContent=s;return d.innerHTML};
function add(text,who='bot',x={}){let e=document.createElement('div');e.className='message '+who;let h=who==='bot'?'<div class="avatar">✦</div>':'';h+=`<div class="bubble">${text}`;if(x.tools?.length)h+=`<details open><summary>Tool activity (${x.tools.length})</summary>${x.tools.map(t=>`<div class="tool">✓ <b>${esc(t.name)}</b> — ${esc(t.label)}</div>`).join('')}</details>`;if(x.sources?.length)h+=`<div class="sources"><b>Evidence</b>${x.sources.map(s=>`<span>${esc(s.title)} · authority ${s.authority}/100</span>`).join('')}</div>`;if(x.confidence)h+=`<div class="confidence ${x.confidence}">${x.confidence} confidence</div>`;if(x.action)h+=`<button class="action">Prepare ${x.action.type.replace('_',' ')}</button>`;e.innerHTML=h+'</div>';chat.append(e);chat.scrollTop=chat.scrollHeight;if(x.action)e.querySelector('.action').onclick=()=>prepare(x.action)}
async function send(text=input.value.trim()){if(!text)return;add(esc(text),'user');input.value='';let r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,role:role.value,account:account.value})}),d=await r.json();add(`<p>${esc(d.answer)}</p>`,'bot',d)}
async function prepare(a){let r=await fetch('/api/actions/prepare',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...a,role:role.value,account:account.value})}),d=await r.json();if(!r.ok)return add(`<p>${esc(d.error)}</p>`);token=d.token;add(`<b>Review before action</b><p>${esc(d.preview.summary)}</p><small>No state has changed.</small><p><button id="confirmAction" class="confirm">Confirm and execute</button></p>`);$('#confirmAction').onclick=confirm}
async function confirm(){let r=await fetch('/api/actions/confirm',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,confirm:true})}),d=await r.json();add(r.ok?`<b>Action ${esc(d.id)} created</b><p>An audit record was saved.</p>`:`<p>${esc(d.error)}</p>`)}
async function radar(){if(role.value==='customer'){alerts.innerHTML='<div class="empty">Issue Radar requires an authorised support or operations role.</div>';metrics.innerHTML='';return}let r=await fetch('/api/issues',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({role:role.value,account:account.value})}),d=await r.json();metrics.innerHTML=Object.entries(d.summary).map(([k,v])=>`<div><strong>${v}</strong><span>${k.replace(/([A-Z])/g,' $1')}</span></div>`).join('');alerts.innerHTML=d.alerts.map(a=>`<article class="${a.level}"><b>${esc(a.title)}</b><p>${esc(a.detail)}</p><span>${a.level}</span></article>`).join('')}
$('#send').onclick=()=>send();input.onkeydown=e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}};document.querySelectorAll('.chips button').forEach(b=>b.onclick=()=>send(b.textContent));document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.view').forEach(x=>x.classList.add('hidden'));$('#'+b.dataset.view).classList.remove('hidden');$('#title').textContent=b.dataset.view==='radar'?'Issue radar':'Support assistant';if(b.dataset.view==='radar')radar()});$('#refresh').onclick=radar;
